{
    int number;
    printf ("Enter an integer:");
    scanf("%d",&number);
    if(number<0){
        printf("you entered %d.\n",number);
    
    }
    printf("The if statement is easy .");
    return 0;
}
